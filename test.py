


stats = {'# of steps': 0,
                '# of live agents': 0,
                '# of foods': 0,
                'Avg of energy': 0,
                '# of Gen': 0}


text = "\n".join(f"{k}: {v}" for k, v in stats.items())

print(repr(text))
